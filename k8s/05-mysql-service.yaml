apiVersion: v1
kind: Service
metadata:
  name: mysql-service
  namespace: cnas
spec:
  # Headless service gives the StatefulSet a stable network identity.
  clusterIP: None
  publishNotReadyAddresses: false
  selector:
    app: mysql
  ports:
    - name: mysql
      port: 3306
      targetPort: mysql
