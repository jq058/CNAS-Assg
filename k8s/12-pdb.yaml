apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: php-app-pdb
  namespace: cnas
spec:
  # PDBs protect only against voluntary disruptions (for example node drains),
  # not abrupt node or host failures.
  maxUnavailable: 1
  selector:
    matchLabels:
      app: php-app
